# `chart-discovery` Terraform Module

This module allows Platz to get ECR notifications.

The reason this is a separate module is in case Platz and the ECR registry are located in two different AWS accounts.

